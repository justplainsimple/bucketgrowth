name: Run Gosec
on:
  push:
  pull_request:
jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      GO111MODULE: on
    steps:
      - name: Install Go
        uses: actions/setup-go@v2
      - name: Checkout Source
        uses: actions/checkout@v2
      - name: Run tests
        run: make test

